import React from 'react';

const Login = React.lazy(() => import('./components/Login'));

const routes = [
    {
        path: '/login',
        exact: false,
        component:
    }
]

export default routes;
